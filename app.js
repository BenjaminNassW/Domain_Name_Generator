
let pronoun = ['the','our'];
let adj = ['great', 'big' ];
let noun = ['jogger','racoon'];
let domain = ['.com', '.io', '.net', '.us']
for(let i=0; i<=1; i++){
    for(let j=0; j<=1; j++){
        for(let k=0; k<=1; k++){
            for(let l=0; l<=3; l++){
            console.log(pronoun[i]+adj[j]+noun[k]+domain[l]);
            }
}
}
}